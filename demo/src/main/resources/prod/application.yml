#设置spring-boot启动的端口号
server:
  port: 9000

my:
  name: 许朋
  age: 25
  number:  ${random.int}
  uuid : ${random.uuid}
  max: ${random.int(10)}
  value: ${random.value}
  greeting: hi,i'm  ${my.name}